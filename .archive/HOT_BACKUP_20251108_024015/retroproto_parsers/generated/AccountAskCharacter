use serde::{Serialize, Deserialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AccountAskCharacterMigration {}

pub fn parse_AccountAskCharacterMigration(payload: &str) -> Result<AccountAskCharacterMigration, String> {
    // This Go message is struct{}; payload is expected to be empty.
    let p = payload.trim_matches('\0').trim();
    if p.is_empty() {
        return Ok(AccountAskCharacterMigration {});
    }
    // Some variants ignore payload in Go; we still succeed but you can switch to strict:
    // return Err(format!("expected empty payload for AccountAskCharacterMigration, got: {}", p));
    Ok(AccountAskCharacterMigration {})
}

pub fn account_ask_character_migration_to_json(m: &AccountAskCharacterMigration) -> serde_json::Value {
    serde_json::json!({})
}